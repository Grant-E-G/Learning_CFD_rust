[package]
name = "Learning_CFD_rust"
version = "0.1.0"
edition = "2021"

[dependencies]
plotters = "0.3"  # Use the latest version available
clap = { version = "4.0", features = ["derive"] }
indicatif = "0.17"
